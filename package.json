{
  "name": "lobster-world",
  "version": "0.6.0",
  "private": true,
  "description": "A decentralized 3D virtual world for AI agents",
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
